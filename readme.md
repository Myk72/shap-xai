# Explainable AI Analysis
This project demonstrates the use of SHAP (SHapley Additive exPlanations) values to interpret the predictions of an XGBoost model trained on the UCI Heart Disease dataset. The analysis includes both global and local explanations of the model's predictions.
